# 本项目依赖清单（以后装环境/部署时一条命令搞定：pip install -r requirements.txt）
fastapi
uvicorn[standard]
langchain
langchain-community
langchain-alibaba
dashscope
python-dotenv
pymilvus[milvus_lite]
sqlalchemy
bcrypt
PyJWT
python-multipart
pypdf

# MySQL（第 8 步：业务库从 SQLite 换成 MySQL，走 Docker compose）
pymysql
cryptography

# 检索增强（BM25 关键词 + 语义排名融合重排）
jieba
rank_bm25
